{
    "require-dev": {
        "phpunit/phpunit": "^9.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "."
        }
    },
    "scripts": {
        "test": "composer install --dev && vendor/bin/phpunit tests",
        "test-only": "vendor/bin/phpunit tests"
    }
} 
